apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "svelte-rings.fullname" . }}-test-connection"
  labels:
    {{- include "svelte-rings.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "svelte-rings.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
